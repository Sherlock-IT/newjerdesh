version: '3'

services:
    web:
        build: ./jerdesh
        command: python manage.py runserver 0.0.0.0:8000
        volumes:
             - ./jerdesh/:/usr/src/jerdesh
        ports:
             - 8000:8000
        env_file:
             - ./.env.dev
